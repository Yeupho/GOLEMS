<div class="imaginary_container">
  <table class="table table-hover">
    <thead>
    <tr>
      <th>Event</th>
      <th>Date</th>
      <th>Time</th>
      <th>Children</th>
      <th>Event Size</th>
      <th>Co-Host</th>
    </tr>
    </thead>

    <tbody>
    <% @hosting.each do |h| %>
        <tr>
          <td><%= h.event.event_name %></td>
          <td><%= h.event.event_date.strftime("%-m/%-d/%y") %></td>
          <td><%= h.event.start_time.strftime("%l:%M %p") %> - <%= h.event.end_time.strftime("%l:%M %p") %></td>
          <td></td>
          <td></td>
          <td></td>
        </tr>
    <% end %>
    </tbody>
  </table>
</div>