<%= form_for(state) do |f| %>
  <% if state.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(state.errors.count, "error") %> prohibited this state from being saved:</h2>

      <ul>
      <% state.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <div>
      <%= f.label :state_name %>
    <div style="width:200px;"><%= f.text_field :state_name, class: 'form-control' %></div>

      <%= f.label :state_abbrev %>
      <div style="width:200px;"><%= f.text_field :state_abbrev, class: 'form-control' %></div>
    </div>
    <br>
    <div class="row footer">
      <%= f.submit class: "btn btn-primary", redirect_to: '/admin#locations_tab' %>
    </div>


<% end %>
