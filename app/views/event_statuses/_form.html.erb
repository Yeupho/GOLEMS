<%= form_for(event_status) do |f| %>
  <% if event_status.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(event_status.errors.count, "error") %> prohibited this event_status from being saved:</h2>

      <ul>
      <% event_status.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :event_status_desc %>
    <%= f.text_field :event_status_desc %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
