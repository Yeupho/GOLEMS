<%= form_for(customer_status) do |f| %>
  <% if customer_status.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(customer_status.errors.count, "error") %> prohibited this customer_status from being saved:</h2>

      <ul>
      <% customer_status.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :customer_status_description %>
    <div style="width:200px;"><%= f.text_field :customer_status_description,class:'form-control' %>
    </div>
  </div>
  <br>
    <div class="row footer">
      <div>
        <%= f.submit class: "btn btn-primary", redirect_to: '/admin#status_tab' %>
      </div>
    </div>
    <br>
<% end %>
