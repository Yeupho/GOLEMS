<div class="row text-right">
  <div class="col-lg-6">
    <button type="button" class="btn btn-primary btn-md show-btn" data-toggle="modal" data-target="#editModal">
      <span class="glyphicon glyphicon-edit"></span>
      <br>
      Edit
    </button>
  </div>
  <div class="col-lg-6">
    <% if @customer_event.event.event_type_id == 7 %>
        <%= link_to '<span class="glyphicon glyphicon-remove-sign"></span><br>Back'.html_safe, walk_ins_path, class: "btn btn-primary btn-md show-btn", method: :get %>
        <% else %>
    <%= link_to '<span class="glyphicon glyphicon-remove-sign"></span><br>Back'.html_safe, params[:customer_event_history], class: "btn btn-primary btn-md show-btn", method: :get %>
        <% end %>
  </div>
</div>

<%= render 'customer_events/show/edit_modal' %>
