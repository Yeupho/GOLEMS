<%= form_for(product_type) do |f| %>
  <% if product_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(product_type.errors.count, "error") %> prohibited this product_type from being saved:</h2>

      <ul>
      <% product_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <div class="field">
      <%= f.label 'Product Type Name' %>
      <div style="width:200px"><%= f.text_field :product_type_desc,class: 'form-control' %></div>
    </div>
    <br>
    <div class="row footer">
      <%= f.submit class: "btn btn-primary", redirect_to: '/admin#locations_tab' %>
    </div>
<% end %>
