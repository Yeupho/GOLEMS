<%= form_for(country) do |f| %>
  <% if country.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(country.errors.count, "error") %> prohibited this country from being saved:</h2>

      <ul>
      <% country.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <div>
      <%= f.label :country_name %>
      <div style="width:200px;"><%= f.text_field :country_name, class: 'form-control' %></div>

      <%= f.label 'Country Abbreviation' %>
      <div style="width:200px;"><%= f.text_field :country_abbrev, class: 'form-control' %></div>
    </div>
    <br>
    <div class="row footer">
      <%= f.submit class: "btn btn-primary", redirect_to: '/admin#locations_tab' %>
    </div>
<% end %>
