<%= form_for(employee_event) do |f| %>
  <% if employee_event.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(employee_event.errors.count, "error") %> prohibited this employee_event from being saved:</h2>

      <ul>
      <% employee_event.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :employee_id %>
    <%= f.number_field :employee_id %>
  </div>

  <div class="field">
    <%= f.label :event_id %>
    <%= f.number_field :event_id %>
  </div>

  <div class="field">
    <%= f.label :archive %>
    <%= f.check_box :archive %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
